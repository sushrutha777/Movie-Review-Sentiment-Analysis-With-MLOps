from setuptools import setup, find_packages

setup(
    name="movie_sentiment_mlops",
    version="0.1.0",
    packages=find_packages(where="."),
    package_dir={"": "."},
    install_requires=[],
)
